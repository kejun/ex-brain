# Maintain Skill

## Daily Checklist
1. `ebrain stats` 查看总量与增长。
2. `ebrain list --limit 20` 检查最近更新质量。
3. `ebrain embed --all` 刷新新增文档向量。
4. `ebrain export --dir ./export` 做可逆导出备份。

## Quality Rules
- 页面必须有清晰标题和类型。
- 时间线条目优先结构化写入。
- 新页面写入后补全至少一个标签。
